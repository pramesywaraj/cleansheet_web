import React from 'react';

import styles from './styles.module.scss';

const DemoTag = () => {
  return (
    <div className={styles["demo-tag-container"]}>
      <p>This is just a DEMO</p>
    </div>
  );
};

export default DemoTag;